/// Centralized asset path constants.
///
/// Keeps all asset references in one place so that path changes
/// only need to be updated here.
class AssetConstants {
  AssetConstants._();

  static const String weatherIcon = 'assets/images/weather_icon.png';
}
